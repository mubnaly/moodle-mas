# Custom PostgreSQL configuration
# This file is mounted into the container (optional)

# Connection settings
# max_connections = 200

# Memory settings
# shared_buffers = 256MB
# effective_cache_size = 768MB
# work_mem = 4MB

# Write-ahead logging
# wal_buffers = 16MB
# checkpoint_completion_target = 0.9

# Logging (for debugging)
# log_statement = 'none'
# log_min_duration_statement = 1000
